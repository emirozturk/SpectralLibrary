import 'package:flutter/material.dart';

class DrawPlotPage extends StatelessWidget {
  const DrawPlotPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("");
  }
}
